#utiliser une image de base Ubuntu 18.04 car compatible avec python 3.6 car les dernières non
FROM ubuntu:18.04

#installation des dépendances nécessaires dont python 3.6 et gcc nécessaires et compatible avec cutadapt 1.11
RUN apt-get update && apt-get install -y \
    build-essential \
    python3.6 \
    python3.6-dev \
    python3-pip \
    python3-setuptools \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

#installation de cutadapt
RUN pip3 install cutadapt==1.11


#vérifier l'installation de Cutadapt
RUN cutadapt --version

#entrée par défaut
CMD ["/bin/bash"]
